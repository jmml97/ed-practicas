StackMax:

- Documentar métodos de stack_max.hpp
- Editar formato de lectura de stack_max.cpp (añadir símbolo $ para diferenciar).
  (ponerlo en la documentación)
- Añadir comparator (functor) a StackMax (poner template en Element y StackMax)
  (ponerlo en la documentación)

QueueMax:

- Archivo test_stack.cpp en src/test
- Repasar implementación + probar.

Proyecto:

- Añadir -DNDEBUG a Makefile cuando vayamos a entregarlo (para compilar sin asserts).
- Asegurarse de que funcionan las tres implementaciones.
